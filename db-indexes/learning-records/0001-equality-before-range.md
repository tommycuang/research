# Equality before range narrows B-tree work

The learner predicted and verified that a leading wallet equality followed by a timestamp range scans far fewer index pages than the reversed order. Future lessons can assume they understand that later index conditions may reject entries without narrowing the contiguous range traversed.
